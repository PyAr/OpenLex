{{extend 'layout.html'}}
<h1>Expedientes</h1>
{{if expte:}}
    <div class='container'>
        <div class="col-md-3 well well-lg">
            {{=expte}}
        </div>
        <div class="col-md-9">
            {{=grid}}
        </div>
   </div>

{{else:}}
        {{=grid}}
{{pass}}
